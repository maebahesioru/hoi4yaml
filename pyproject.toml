[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hoi4yaml"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pyyaml"]

[project.scripts]
hoi4yaml = "generate:main"

[tool.setuptools]
py-modules = ["generate"]
